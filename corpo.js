function atribuir() {
    var sal = window.document.querySelector('input#salario')
    var destinoo = window.document.querySelector('div#destino')
    var total = Number (sal.value)
    destino.innerHTML = `seu total é de ${total}`
   
}
